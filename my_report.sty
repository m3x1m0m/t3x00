%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%																				%%
%% File name: 		my_report.sty												%%
%% Project name:	Hochleistungsantenne										%%
%% Type of work:	T3X00 project work											%%
%% Author:			Sarah Brückner, Maximilian Stiefel, Hannes Bohnengel		%%
%% Date:			16th May 2016		     									%%
%% University:		DHBW Ravensburg Campus Friedrichshafen						%%
%% Comments:		Created in gedit with tab width = 4							%%
%%																				%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%---- macros ---------------------------------------------------------------------

% User defined length of new paragraph
\newcommand{\newpar}{\\[1em]}

% User defined way of emphasizing words
\newcommand{\myemph}[1]{\textsl{#1}}

% Specify user defined symbol
\newcommand{\myvsymbol}{\includegraphics[width=0.7em]{v-symbol}}

% User defined spacing before and after titles of toc, lof and lot
% Compare with titlespacing of chapter !
\newcommand{\myspacebeforetitle}{-2em}
\newcommand{\myspaceaftertitle}{1em}

% Alternative 1 (wide)
%\newcommand{\myspacebeforechapintoc}{1.5em}
%\newcommand{\myspacebeforesecintoc}{1em}
%\newcommand{\myspacebeforesubsecintoc}{0.4em}

% Alternative 2 (narrow)
\newcommand{\myspacebeforechapintoc}{0.5em}
\newcommand{\myspacebeforesecintoc}{0.4em}
\newcommand{\myspacebeforesubsecintoc}{0.3em}

%---- packages -------------------------------------------------------------------

% Provide a package to use in a .tex file
\ProvidesPackage{my_report}

% To amend spacing of titles
\usepackage{titlesec} 

% Define font encodi\usepackage{hyperref}ng for output
\usepackage[T1]{fontenc}

% Define font encoding for input (e.g. ä, ö, ü)
\usepackage[utf8]{inputenc}

% Define language for hyphenation
\usepackage[ngerman]{babel}
											
% DinA4 paper, margins according to DHBW
\usepackage[head=24pt,a4paper,lmargin={2.5cm},rmargin={2.5cm},tmargin={2.5cm},
bmargin={2.5cm}]{geometry} 

% To use mathematic symbols (to display symbols)
\usepackage{amssymb}

% To use mathematic environments (to display equations)
\usepackage{amsthm}

% To display images
\usepackage{graphicx}

% To use wrap figures (surrounded by text)
%\usepackage{wrapfig}

% To use captions
\usepackage{caption}
	
% For citation
\usepackage{csquotes}
\usepackage[style=ieee,backend=bibtex]{biblatex}
\addbibresource{bib/bibliography.bib}
%\renewcommand{\bibfont}{\small}	% to change the font size of the bib

% To use tables
\usepackage{tabularx}

% To use abbreviations
% Either
\usepackage[printonlyused,nohyperlinks]{acronym}
% Or
%\usepackage[toc,nonumberlist]{glossaries} 
%\makeglossaries   
   
% To create table of contents and list of figures/tables
\usepackage{tocloft}

%Adding list of figures and list of tables to the toc in a proper way
\usepackage[nottoc]{tocbibind}   


%Head and Foot
\usepackage{fancyhdr}
\lhead{\includegraphics[width=2cm]{dhbw_small}}\chead{}\rhead{\nouppercase\rightmark}
\lfoot{}\cfoot{\thepage}\rfoot{}
\pagestyle{fancy}
%\renewcommand{\headheight}{24pt}

% For colored text
\usepackage{color}

%---- Different defines ----------------------------------------------------------

% Define line spacing
\linespread{1.5}

% To go on with roman page numbers after main part
\newcounter{romanPagenumber}

% Define path of images
\graphicspath{ {images/} }

% Set indentation of new paragraphs
\setlength{\parindent}{0pt}

% For colored text
%\usepackage{color} 

%\titleformat{\chapter}{\bf\Huge}{\thechapter\quad}{0em}{}

%\titlespacing*{\chapter}{0pt}{-4em}{1.5em}{} %{<left>}{<before>}{<after>}{<right>}

% Format the style of chapter
\titleformat%
{\chapter}%
[block]%
{\bfseries\Huge}%
{\thechapter\hspace{1cm}}%
{0pt}%
{\bfseries\Huge}%

% Format the style of section
\titleformat%
{\section}%
[block]%
{\bfseries\Large}%
{\thesection\hspace{0.5cm}}%
{0pt}%
{\bfseries\Large}%

% Format the style of subsection
\titleformat%
{\subsection}%
[block]%
{\bfseries\large}%
{\thesubsection\hspace{0.5cm}}%
{0pt}%
{\bfseries\large}%

% Set spacing of chapter
\titlespacing*{\chapter}{0pt}{-4em}{1.5em}{} %{<left>}{<before>}{<after>}{<right>}

% Set spacing of section
%\titlespacing*{\section}{0pt}{-4em}{1.5em}{} %{<left>}{<before>}{<after>}{<right>}

% Set spacing of subsection
\titlespacing*{\subsection}{0pt}{-0.5em}{0.5em}{} %{<left>}{<before>}{<after>}{<right>}

% http://www.golatex.de/viewtopic,p,51871.html
% ftp://ftp.fu-berlin.de/tex/CTAN/macros/latex/contrib/titlesec/titlesec.pdf

%Optional, page number and chapter is stated in the header
%\pagestyle{headings}  

% ---- Table of contents ---------------------------------------------------------

% Set spacing before title of toc
\setlength\cftbeforetoctitleskip{\myspacebeforetitle}

% Set spacing after title of toc
\setlength\cftaftertoctitleskip{\myspaceaftertitle}

% Set spacing in toc for chapter
\setlength\cftbeforechapskip{\myspacebeforechapintoc}

% Set spacing in toc for section
\setlength\cftbeforesecskip{\myspacebeforesecintoc}

% Set spacing in toc for subsection
\setlength\cftbeforesubsecskip{\myspacebeforesubsecintoc}

% Set indentation (and numwidth) in toc for chapter
\cftsetindents{chapter}{0pt}{1em}

% Set indentation (and numwidth) in toc for section
\cftsetindents{section}{0pt}{2em}

% Set indentation (and numwidth) in toc for subsection
\cftsetindents{subsection}{2em}{3em}

% For filling the toc with dots
\renewcommand\cftchapleader{\cftdotfill{\cftdotsep}}
%\renewcommand\cftsecleader{\cftdotfill{\cftdotsep}}

% ---- List of figures -----------------------------------------------------------

% Set spacing before title of toc
\setlength\cftbeforeloftitleskip{\myspacebeforetitle}

% Set spacing after title of toc
\setlength\cftafterloftitleskip{\myspaceaftertitle}

% Set indentation of lof
\setlength{\cftfigindent}{0pt}

% ---- List of tables ------------------------------------------------------------

% Set spacing before title of toc
\setlength\cftbeforelottitleskip{\myspacebeforetitle}

% Set spacing after title of toc
\setlength\cftafterlottitleskip{\myspaceaftertitle}

% Set indentation of lot
\setlength{\cfttabindent}{0pt}
